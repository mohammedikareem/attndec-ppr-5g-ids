data_root: "../../data"
dataset: "InSDN"
seed: 42
split: {train: 0.70, val: 0.15, test: 0.15}

model:
  name: "model"
  latent_dim: 64
  attn_heads: 4
  dropout: 0.2

train:
  batch_size: 512
  epochs: 5
  lr: 1e-3
  optimizer: "adam"
